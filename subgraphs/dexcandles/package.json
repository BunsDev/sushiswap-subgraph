{
    "name": "dexcandles",
    "license": "MIT",
    "version": "1.0.0",
    "author": "SoulSwap",
    "scripts": {
        "codegen": "graph codegen subgraph.yaml",
        "build": "graph build subgraph.yaml",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ soulswapfinance/dexcandles-avalanche subgraph.yaml",
        "create-local": "graph create --node http://localhost:8020/ /soulswapfinance/dexcandles-avalanche",
        "remove-local": "graph remove --node http://localhost:8020/ /soulswapfinance/dexcandles-avalanche",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 /soulswapfinance/dexcandles-avalanche",
        "prepare:avalanche": "mustache config/avalanche.json template.yaml > subgraph.yaml"
    }
}
